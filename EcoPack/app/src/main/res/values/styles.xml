<resources>
    <!-- BASE APPLICATION THEME -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/dark_mint</item>
        <item name="colorPrimaryDark">@color/mint</item>
        <item name="colorAccent">@color/pale_beige</item>
        <item name="android:textColorHint">@color/white_smoke</item>
    </style>

    <!-- USED TO PREVENT THE APP FROM USING THE NATIVE ACTIONBAR CLASS TO PROVIDE THE TOOLBAR -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="NavigationBarTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/dark_grey</item>
        <item name="colorAccent">@color/pale_beige</item>
    </style>

    <style name="NavigationBarTheme.ActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <!-- ALERT DIALOG -->
    <style name="AlertDialogMap" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColor">@color/dark_mint</item>
        <item name="android:textColorPrimary">@color/dark_grey</item>
    </style>
</resources>