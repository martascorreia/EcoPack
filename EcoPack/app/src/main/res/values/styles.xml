<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/darkMenta</item>
        <item name="colorPrimaryDark">@color/menta</item>
        <item name="colorAccent">@color/paleBeige</item>
    </style>

    <style name="NavegationTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/darkGrey</item>
        <item name="colorAccent">@color/paleBeige</item>
    </style>

</resources>